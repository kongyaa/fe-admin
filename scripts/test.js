const { Client } = require("@notionhq/client"); async function testNotion() { console.log("Starting Notion API test..."); try { const notion = new Client({ auth: process.env.NOTION_KEY }); console.log("Attempting to query the database..."); const response = await notion.databases.query({ database_id: process.env.NOTION_DATABASE_ID }); console.log("Database query successful!"); console.log("Found", response.results.length, "pages"); } catch (error) { console.error("Error occurred:", error); process.exit(1); } } console.log("Test script starting..."); testNotion().catch(console.error);
